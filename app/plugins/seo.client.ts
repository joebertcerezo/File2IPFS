export default defineNuxtPlugin(() => {
  // Initialize SEO-related functionality
  if (process.client) {
    // Add any client-side SEO enhancements here
    console.log("File2IPFS SEO initialized");
  }
});
